extends layout

block content
  h1= title
  #new_task
    form(ng-submit="addTask(task)")
      div
        label(for="newTaskContent") New Task
        input(type="text", name="task_content", id="newTaskContent", placeholder="Task content", ng-model="task.content")
      div#newTaskSubmit
        input(type="submit", value="Add")
      div
        | Content: {{ task.content }}
  #tasks
    p There are {{ tasks.length }} tasks!
    .task(ng-repeat="task in tasks" id="{{ task._id }}")
      span.removeTask
        a(ng-href="#" ng-click="deleteTask(task._id)") X
      .content
        {{ task.content }}
    
    
    
    // - each task in tasks
    //  div.task(id=task._id)
    //    span.removeTask
    //      a(href="#") X
    //    div.content
    //      a(href="/todo/" + task._id)!= task.content
    
